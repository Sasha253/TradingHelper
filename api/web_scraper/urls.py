from django.urls import path
from .views import UserAccountView, SignUpView, LoginView

urlpatterns = [
    path('useraccount' ViewPoint.as_view())
    path('signup' ShareData.as_view())
    path('login' TradingView.as_view())
]